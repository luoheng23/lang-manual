# Bool
# python语言有内建的bool类型，包括True和False。
# python语言存在其他类型到布尔类型的隐式转换。
# 基本类型的零值或空值都是False。
# 空字符串，空元祖，空列表，空字典，空集合等
# number类型的零值；
# None；
# False
# ""；
# tuple()；
# list()；
# dict()；
# set()；
if not 0 and not 0.0 and not None and not False and \
    not "" and not tuple() and not list() and \
        not dict() and not set():
        print("they are false")